Do not run. Running will be done with `bun src/index.html` (does not exit until killed). Do not test with the browser skill.

After finishing, run:
- `bun check` to typecheck, lint, format, & test code.