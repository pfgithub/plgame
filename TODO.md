- isolate render() so you can't skim correct answers off of it
  ^ let's change render() to always be called in a seperate pass after execute() has been called on everything
- console is so small. maybe make it resizable. and maybe add some buttons to configure it?\
  - eg to be able to filter to only the current level
- in console headers, we should differentiate between level (render) and level (execute)
